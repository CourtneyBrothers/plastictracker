html
  head
    title stats!
    include stylesheets.pug
  body
    include ./partials/nav.pug
    
    
    unless !anonymous
      h1 you don't have any SUP stats yet!

    
    block content
      h1 what's good?
      h2 these are your totals for SUP consumption

  - let resultArray = result[0]
  - console.log(resultArray, "result array")
  //sequelize query returns array of anonymous obj w/ plastic id and quantity
  each anonymous in resultArray
    a(href=`/sup/${anonymous.ReuseThisPlasticId}`)
      li type id: #{anonymous.ReuseThisPlasticId} quantity: #{anonymous.quantity}
   

