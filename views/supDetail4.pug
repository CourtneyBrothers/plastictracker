html
  head
    title you rock!
    include stylesheets.pug
  body
    include ./partials/nav.pug
    div(class="container" class="error")
          div(class="postition-relative overflow-hidden p-3  p-md-5 m-md3 text-center")
                  div(class="col-md-5 p-lg-5 mx-auto my-5")
                    h1(class="display-4 font-weight-bold") The Last Straw</br> 
                    a(class="btn btn-outline-info btn-lg" href=`/welcome` role="button") make some waves



  
    block content
      - let resultArray = result[0]
      - console.log(resultArray, "result array")
      - console.log(session.passport, "session.passport")
  //sequelize query returns array of anonymous obj w/ plastic id and quantity
  each anonymous in resultArray
      div(class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center")
        h1(class="display-4") #{anonymous.label} total saved quantity:#{anonymous.quantity}
        p(class="lead") TODO FILL IN THIS W/ UX 
        - let quantity = +`${anonymous.quantity}`
        -let quantArray = new Array(quantity).fill('plastic')
        .container
          div(class="row p-5" class="row justify-content-center")
            each plastic, index in quantArray
              div(class="card" class="reuse" style="width: 10rem;")
                img(src="/images/ecologism.svg" class="rounded-circle" width="140" height="140" class="recycleThis" id=`sup${index}`)
                div(class="card-body")
                button(class="btn btn-outline-info btn-lg" class="reuse" class="reuseBtn" id=`${index}`) reuse!
                button(class="btn btn-outline-success btn-lg" class="recycle" class="recycleBtn" id=`${index}`) recyle!


script 
  include ../node_modules/jquery/dist/jquery.min.js
  include ../client/clientapp/sup.js